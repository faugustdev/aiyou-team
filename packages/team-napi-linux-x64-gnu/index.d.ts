// Type stubs for @aiyou-dev/team-napi-linux-x64-gnu.
// The runtime is provided by the native .node artifact; this file only carries
// the surface that the @aiyou-dev/team JavaScript layer consumes.

export function validateTeamManifestFile(filePath: string): string;
export function validateTeamPolicyFile(filePath: string): string;
export function listImplementedAiyouTeamPluginTools(): unknown[];
